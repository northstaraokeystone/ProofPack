"""ProofPack performance benchmarks.

Run all benchmarks:
    pytest benchmarks/ --benchmark-only

Run with warmup:
    pytest benchmarks/ --benchmark-only --benchmark-warmup=on

Generate JSON report:
    pytest benchmarks/ --benchmark-only --benchmark-json=results.json
"""
